import { ColorearUsuarioDirective } from './colorear-usuario.directive';

describe('ColorearUsuarioDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorearUsuarioDirective();
    expect(directive).toBeTruthy();
  });
});
