import { MostrarEspecialidadDirective } from './mostrar-especialidad.directive';

describe('MostrarEspecialidadDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarEspecialidadDirective();
    expect(directive).toBeTruthy();
  });
});
